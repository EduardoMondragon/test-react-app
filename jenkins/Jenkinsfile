pipeline{
    agent any 
    tools {nodejs 'NODEJS'}
    stages{
        stage('Fetch dependencies'){
            steps{
                sh 'npm install'
            }
        }
        stage('Test the testing files'){
            steps{
                sh 'npm test'
            }
        }
    }
}